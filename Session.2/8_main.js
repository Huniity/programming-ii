// O(2n+5) and O(n) are basically the same since they increase linearly. 
// The only thing that influence the complexity
//      is the number of operations. More operations equals more time to process.
// So O(2n+5) will always simplyfies to O(2n).